# Recetas médicas descargables — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development.

**Goal:** El médico genera recetas imprimibles/descargables (membrete + medicamentos), guardadas y visibles en el expediente y en el portal del paciente.

**Architecture:** Vanilla PHP + MariaDB + HTML/JS inline. Tabla `recetas` (medicamentos en JSON). 2 endpoints + extensiones a `medico_expediente`/`paciente_perfil`. Página `receta.html` optimizada para impresión (guardar como PDF del navegador; sin librerías).

**Entorno NAS (igual que planes previos):** `plink -pw "<pass>" -hostkey "SHA256:QoI4JtkGYcHZLypBwZ5m+/NJiGzdlQS0uCRFmHYfEN8" pbaquerizo@192.168.0.116`, técnica base64, `sudo -S`, PHP como http `sudo -u http /usr/local/bin/php82`, API interna `curl -H 'Host: medicvip.org' http://127.0.0.1/api.php`, deploy por `pscp` (644, legible por http), `php -l` en el NAS.

**Plantillas a espejar:** `expediente-paciente.html` (tabs + modal, estilo clínico), `paciente-portal.html` (secciones), `medico-portal.html` (paleta/fonts).

---

## Task 1: Migración DB

**Files:** migración NAS + `schema.sql`.

- [ ] **Step 1: Aplicar en el NAS (base `mediconline`)**
```sql
CREATE TABLE IF NOT EXISTS `recetas` (
  `id` int(10) unsigned NOT NULL AUTO_INCREMENT,
  `paciente_id` int(10) unsigned NOT NULL,
  `medico_id` int(10) unsigned NOT NULL,
  `reserva_id` int(10) unsigned DEFAULT NULL,
  `diagnostico` text DEFAULT NULL,
  `indicaciones` text DEFAULT NULL,
  `items` longtext DEFAULT NULL,
  `fecha_emision` date NOT NULL DEFAULT (curdate()),
  `creado_en` datetime NOT NULL DEFAULT current_timestamp(),
  PRIMARY KEY (`id`),
  KEY `idx_paciente` (`paciente_id`),
  CONSTRAINT `fk_receta_paciente` FOREIGN KEY (`paciente_id`) REFERENCES `pacientes` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```
- [ ] **Step 2: Verificar** — `SHOW TABLES LIKE 'recetas';` + `SHOW COLUMNS FROM recetas;`.
- [ ] **Step 3: Reflejar en `schema.sql`** y commit.

---

## Task 2: Backend — recetas

**Files:** `api.php`.

- [ ] **Step 1: Insertar funciones** (en zona de funciones, p.ej. tras el bloque de expediente):
```php
// ── RECETAS ───────────────────────────────────────────────────────────────────
function recetaFolio($id): string { return 'REC-' . str_pad((string)$id, 6, '0', STR_PAD_LEFT); }

function medicoRecetaCrear(): void {
    $medicoId = checkMedico(); $data = json_decode(file_get_contents('php://input'), true);
    $pid = (int)($data['paciente_id'] ?? 0); if (!$pid) jsonError('Falta paciente_id');
    checkRelacionMedicoPaciente($medicoId, $pid);
    $items = $data['items'] ?? null;
    if (!is_array($items) || count($items) === 0) jsonError('La receta debe tener al menos un medicamento');
    $limpios = [];
    foreach ($items as $it) {
        $med = trim((string)($it['medicamento'] ?? '')); if ($med === '') continue;
        $limpios[] = [
            'medicamento'  => mb_substr($med, 0, 200),
            'dosis'        => mb_substr(trim((string)($it['dosis'] ?? '')), 0, 100),
            'frecuencia'   => mb_substr(trim((string)($it['frecuencia'] ?? '')), 0, 100),
            'duracion'     => mb_substr(trim((string)($it['duracion'] ?? '')), 0, 100),
            'indicaciones' => mb_substr(trim((string)($it['indicaciones'] ?? '')), 0, 300),
        ];
        if (count($limpios) >= 30) break;
    }
    if (!count($limpios)) jsonError('La receta debe tener al menos un medicamento');
    $rid = !empty($data['reserva_id']) ? (int)$data['reserva_id'] : null;
    $diag = mb_substr(trim((string)($data['diagnostico'] ?? '')), 0, 2000);
    $ind  = mb_substr(trim((string)($data['indicaciones'] ?? '')), 0, 2000);
    $json = json_encode($limpios, JSON_UNESCAPED_UNICODE);
    $db = getDB();
    $st = $db->prepare('INSERT INTO recetas (paciente_id,medico_id,reserva_id,diagnostico,indicaciones,items) VALUES (?,?,?,?,?,?)');
    $st->bind_param('iiisss', $pid, $medicoId, $rid, $diag, $ind, $json);
    $st->execute();
    $id = (int)$db->insert_id;
    jsonOk(['id' => $id, 'folio' => recetaFolio($id)]);
}

function recetaVer(): void {
    $data = json_decode(file_get_contents('php://input'), true);
    $rid = (int)($data['receta_id'] ?? 0); if (!$rid) jsonError('Falta receta_id');
    $rec = fetchOne(query('SELECT id,paciente_id,medico_id,diagnostico,indicaciones,items,fecha_emision FROM recetas WHERE id=?', 'i', [$rid]));
    if (!$rec) jsonError('Receta no encontrada', 404);
    if (!empty($_SERVER['HTTP_X_PACIENTE_TOKEN'])) {
        $pidTok = checkPaciente();
        if ((int)$rec['paciente_id'] !== $pidTok) jsonError('No autorizado', 403);
    } else {
        $medicoId = checkMedico();
        checkRelacionMedicoPaciente($medicoId, (int)$rec['paciente_id']);
    }
    $pac = fetchOne(query('SELECT nombre,cedula,fecha_nacimiento,genero FROM pacientes WHERE id=?', 'i', [(int)$rec['paciente_id']]));
    $med = fetchOne(query('SELECT CONCAT(m.titulo," ",m.nombre," ",m.apellido) AS nombre_completo, m.licencia, e.especialidad FROM medicos m LEFT JOIN medico_especialidad e ON e.medico_id=m.id WHERE m.id=?', 'i', [(int)$rec['medico_id']]));
    $edad = null;
    if (!empty($pac['fecha_nacimiento'])) { try { $edad = (new DateTime($pac['fecha_nacimiento']))->diff(new DateTime('now'))->y; } catch (Exception $e) {} }
    jsonOk([
        'folio' => recetaFolio($rec['id']),
        'fecha_emision' => $rec['fecha_emision'],
        'diagnostico' => $rec['diagnostico'],
        'indicaciones' => $rec['indicaciones'],
        'items' => json_decode($rec['items'] ?: '[]', true),
        'paciente' => ['nombre'=>$pac['nombre']??'', 'cedula'=>$pac['cedula']??'', 'fecha_nacimiento'=>$pac['fecha_nacimiento']??null, 'genero'=>$pac['genero']??'', 'edad'=>$edad],
        'medico' => ['nombre_completo'=>$med['nombre_completo']??'', 'especialidad'=>$med['especialidad']??'', 'licencia'=>$med['licencia']??''],
    ]);
}
```

- [ ] **Step 2: Extender `medicoExpediente`** — antes de su `jsonOk($out);`, agregar:
```php
    $st = $db->prepare('SELECT id,fecha_emision,diagnostico,items,creado_en FROM recetas WHERE paciente_id=? ORDER BY creado_en DESC');
    $st->bind_param('i',$pid); $st->execute();
    $recs = $st->get_result()->fetch_all(MYSQLI_ASSOC);
    foreach ($recs as &$r) { $arr=json_decode($r['items']?:'[]',true); $r['num_items']=is_array($arr)?count($arr):0; unset($r['items']); $r['folio']=recetaFolio($r['id']); }
    unset($r);
    $out['recetas'] = $recs;
```

- [ ] **Step 3: Extender `pacientePerfil`** — antes de su `jsonOk($perfil);`, agregar:
```php
    $stR = $db->prepare('SELECT id,fecha_emision,diagnostico,items,creado_en FROM recetas WHERE paciente_id=? ORDER BY creado_en DESC');
    $stR->bind_param('i',$pid); $stR->execute();
    $recsP = $stR->get_result()->fetch_all(MYSQLI_ASSOC);
    foreach ($recsP as &$rp) { $arr=json_decode($rp['items']?:'[]',true); $rp['num_items']=is_array($arr)?count($arr):0; unset($rp['items']); $rp['folio']=recetaFolio($rp['id']); }
    unset($rp);
    $perfil['recetas'] = $recsP;
```

- [ ] **Step 4: Casos en el switch**
```php
        case 'medico_receta_crear': medicoRecetaCrear(); break;
        case 'receta_ver':          recetaVer();          break;
```

- [ ] **Step 5: Auto-revisión** — `medicoRecetaCrear` bind `'iiisss'` (6); `recetaVer` verifica token de paciente O médico (nunca ambos obligatorios); `medicoExpediente`/`pacientePerfil` agregan `recetas` sin romper el `jsonOk`. Llaves balanceadas; 2 `case`.

---

## Task 3: `receta.html` (página nueva — impresión)

**Files:** Create `receta.html`.

- [ ] **Step 1: Construir** (HTML+CSS+JS inline, sin build). Página de **impresión de receta** con membrete profesional. Estética limpia tipo documento médico (fondo blanco, tipografía legible; reusa fonts del sitio: DM Serif Display para el membrete, DM Sans para el cuerpo).
  - Lee `id` de la query (`new URLSearchParams(location.search).get('id')`).
  - Token: `const T = localStorage.getItem('medico_token'); const P = localStorage.getItem('paciente_token');` — usa el que exista. Header: si hay `medico_token` → `X-Medico-Token`; si no y hay `paciente_token` → `X-Paciente-Token`. Si ninguno → mostrar "Inicia sesión para ver la receta".
  - `POST /api.php?action=receta_ver` body `{receta_id: id}` con el header del token → renderiza:
    - **Membrete (arriba):** "MedicOnline" (marca) a la izquierda; a la derecha o debajo: **Dr(a). {medico.nombre_completo}** · {medico.especialidad} · **Reg. médico: {medico.licencia}**.
    - **Fila de paciente/folio:** Paciente: {nombre} · Edad: {edad} años · CI: {cedula} — Folio: {folio} · Fecha: {fecha_emision}.
    - **Rp/** (símbolo grande) y la lista de medicamentos: por cada item → línea "**{medicamento}**" y debajo, en gris, "{dosis} · {frecuencia} · {duracion}" y "{indicaciones}". Numerados.
    - **Indicaciones generales** (si hay) y **Diagnóstico** (si hay).
    - **Línea de firma** al pie: una línea horizontal + "{medico.nombre_completo}" + "Reg. médico {medico.licencia}".
  - Botón **"🖨️ Imprimir / Descargar PDF"** → `window.print()`.
  - CSS `@media print`: ocultar el botón y cualquier nav; márgenes de página carta/A4; el documento a ancho completo. En pantalla, centrar el documento (~800px) con sombra suave.
  - Escapa el contenido dinámico (helper `esc()`).
- [ ] **Step 2: Auto-revisión** — abre como HTML completo; el JS compila (`new Function`); sin token muestra aviso; con token pide `receta_ver` y renderiza; `@media print` oculta el botón.

---

## Task 4: `expediente-paciente.html` — pestaña "Recetas" + "Nueva receta"

**Files:** `expediente-paciente.html`.

- [ ] **Step 1: Nueva pestaña "Recetas"** — agregar un tab "Recetas" al `tablist` existente y su panel (mismo patrón show/hide que las otras pestañas del archivo — LÉELO). El panel:
  - Botón **"➕ Nueva receta"** (abre el modal reutilizable de la página).
  - Lista de `EXP.recetas` (folio, fecha_emision, diagnóstico, `num_items` medicamentos) cada una con enlace **"Ver / Imprimir"** → `window.open('/receta.html?id='+r.id,'_blank')`. Estado vacío: "Sin recetas emitidas".
- [ ] **Step 2: Modal "Nueva receta"** (usa el modal reutilizable de la página):
  - `<select>` de consulta (de `EXP.reservas`, opcional) — al cambiar, pre-llenar el diagnóstico (de la consulta correspondiente en `EXP.consultas` por `reserva_id`) y los renglones de medicamentos a partir de los **tratamientos de esa consulta** (`EXP.tratamientos` con `reserva_id` igual → mapear a renglones medicamento/dosis/frecuencia/duracion). Editables.
  - **Renglones de medicamentos dinámicos**: cada renglón con inputs medicamento*, dosis, frecuencia, duración, indicaciones + botón quitar; botón "＋ Agregar medicamento" agrega un renglón vacío. Empezar con 1 renglón.
  - Campo **Indicaciones generales** (textarea).
  - Botón **"Emitir receta"** → arma `items` = array de los renglones con `medicamento` no vacío; valida ≥1; `POST /api.php?action=medico_receta_crear` con `{paciente_id: pid, reserva_id, diagnostico, indicaciones, items}` y `X-Medico-Token`. Al éxito: cerrar modal, `cargarExpediente()` y `window.open('/receta.html?id='+json.data.id,'_blank')`.
- [ ] **Step 3: Auto-revisión** — el tab cambia; el modal pre-llena de la consulta; agregar/quitar renglones funciona; emite y abre la receta. JS compila.

---

## Task 5: `paciente-portal.html` — sección "Recetas"

**Files:** `paciente-portal.html`.

- [ ] **Step 1:** Agregar nav-item "📄 Recetas" → `showSection('recetas',this)` y una `<section id="section-recetas" class="section">` con `<div id="recetas-lista"></div>` (encabezado con el patrón real `page-title`/`page-sub`).
- [ ] **Step 2:** En `loadPerfil()` poblar `#recetas-lista` desde `<VAR>.recetas` (usar la variable de perfil real):
```javascript
const _rc = document.getElementById('recetas-lista');
if (_rc) {
  const rs = (<VAR>.recetas) || [];
  _rc.innerHTML = rs.length ? rs.map(r => `<div style="border:1px solid var(--border);border-radius:12px;padding:14px;margin-bottom:10px;background:var(--white);display:flex;justify-content:space-between;align-items:center;gap:12px;flex-wrap:wrap">
      <div>
        <div style="font-weight:600">${r.folio||('Receta #'+r.id)}</div>
        <div style="font-size:13px;color:var(--muted)">${r.fecha_emision||''} · ${r.num_items||0} medicamento(s)${r.diagnostico?(' · '+r.diagnostico):''}</div>
      </div>
      <button onclick="window.open('/receta.html?id=${r.id}','_blank')" style="background:#1565C0;border:none;color:#fff;font-size:13px;padding:8px 16px;border-radius:8px;cursor:pointer">Ver / Descargar</button>
    </div>`).join('') : '<p style="color:var(--muted);font-size:14px">Aún no tienes recetas.</p>';
}
```
- [ ] **Step 3: Auto-revisión** — la sección existe; `showSection('recetas')` la muestra; el JS compila.

---

## Task 6: Deploy, verificación E2E y commit

**Files:** deploy de `api.php`, `receta.html`, `expediente-paciente.html`, `paciente-portal.html`.

- [ ] **Step 1: Deploy** por `pscp` de los 4 archivos + `schema.sql` (commit).
- [ ] **Step 2: `php -l api.php`** en el NAS → sin errores.
- [ ] **Step 3: Verificación E2E (curl interno)** — crear paciente de prueba + reserva con médico #22:
  1. Login médico #22 → token.
  2. `medico_receta_crear` con `{paciente_id, reserva_id, diagnostico:"Faringitis", indicaciones:"Reposo", items:[{"medicamento":"Amoxicilina 500mg","dosis":"1 cápsula","frecuencia":"c/8h","duracion":"7 días"}]}` → `{id, folio}`.
  3. `receta_ver` con `X-Medico-Token` `{receta_id}` → devuelve membrete completo (paciente, medico con licencia, items).
  4. `receta_ver` con `X-Paciente-Token` del dueño → OK; con token de otro paciente → 403.
  5. `medico_expediente` → incluye la receta en `recetas`. `paciente_perfil` → idem.
  6. Limpiar datos de prueba.
- [ ] **Step 4: Verificación UI** — desde el expediente, "➕ Nueva receta" pre-llena de una consulta, emite y abre `receta.html` con membrete; el botón imprime (abre diálogo). El portal del paciente lista la receta y la abre.
- [ ] **Step 5: Commit y push** de todo + `schema.sql`.

---

## Criterios de aceptación (del spec)
1. Médico crea receta desde el expediente, pre-llenando de una consulta y editando.
2. Se abre la página imprimible con membrete, paciente, Rp/ + medicamentos, indicaciones, diagnóstico, folio y firma.
3. "Imprimir / Descargar PDF" abre el diálogo del navegador.
4. La receta queda listada en el expediente y re-imprimible.
5. El paciente ve y descarga sus recetas en su portal.
6. Médico sin relación o paciente ajeno → 403.
